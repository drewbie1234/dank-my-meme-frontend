import React from "react";
import styles from "./VotesPage.module.css"; // Adjust the path if needed

const VotesPage = () => {
  return (
    <div className={styles.votesPageContainer}>
      <h1>Votes Page</h1>
      <p>This is the Votes page content, where users see the contests they've voted on.</p>
    </div>
  );
};

export default VotesPage;
